def greet(name):
  """This function greets the user by name.

  Args:
      name: The name of the user to greet (str).

  Returns:
      A string containing the greeting message (str).
  """
  return f"Hello, {name}!"

print(greet.__doc__)